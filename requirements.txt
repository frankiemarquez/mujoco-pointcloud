mujoco>=3.1.0
open3d>=0.18.0
numpy
imageio
matplotlib
mediapy  # used by panda_ik_demo.py; also needs an ffmpeg binary on PATH

# Optional, for real open-vocabulary matching in open_vocab_query.py
# (falls back to a dependency-free color/shape heuristic if not installed):
# torch
# open_clip_torch
# pillow
